const {parse}=require('csv-parse/sync');
(async()=>{for(let y=2015;y<=2026;y++){try{const r=await fetch(`https://pjcrdatosabiertos.blob.core.windows.net/datosabiertos/PJCROD_POLICIALES_V1/PJCROD_POLICIALES_V1-${y}.csv`,{signal:AbortSignal.timeout(30000)});const b=await r.arrayBuffer();let text;try{text=new TextDecoder('utf-8',{fatal:true}).decode(b)}catch{text=new TextDecoder('windows-1252').decode(b)}const rows=parse(text,{bom:true,skip_empty_lines:true,trim:true,relax_column_count:true});const bad=rows.filter(r=>r.length!==11);console.log(JSON.stringify({year:y,rows:rows.length,lengths:[...new Set(rows.map(r=>r.length))],bad:bad.slice(0,2)}))}catch(e){console.log(y,e.message)}}})();
